<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <!-- 연결하기 -->
 <mapper namespace="">
 	<select id="" resultType=""> <!-- id=메소드명을 넣어준다 resultType=model이 있는 경로를 가지고온다  -->
 		SELECT
 			*
 		FROM
 			`store`
 	</select>
 </mapper>